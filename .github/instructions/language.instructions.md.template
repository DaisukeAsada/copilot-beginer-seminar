---
name: <Language Name> Development Rules
description: <言語別Instructionの目的を1行で>
applyTo: "<適用対象のglobパターン>"
---

# <言語名> 開発ガイド

このファイルは `<言語名>` を利用する実装で守るルールを定義します。

## 対象・前提

- 対象言語: `<例: TypeScript / Python / Go / Java>`
- バージョン: `<例: TypeScript 5.x / Python 3.12 / Go 1.23>`
- 対象範囲: `<例: src/**/*.ts / server/**/*.py>`
- 非対象: `<例: 生成物・外部依存ディレクトリ>`

## 目的

- <狙い1: 型安全性/保守性/可読性の担保>
- <狙い2: チーム内の実装スタイル統一>
- <狙い3: 不具合を早期検知できる品質ゲートの整備>

## 必須ルール

### 1. 型・シグネチャ規約

```
// ✅ 正しい例
<typed_function_or_interface_example>

// ❌ 間違い例
<untyped_or_ambiguous_example>
```

- ルール: `<戻り値型明示 / any禁止 / nullable明示 など>`
- 例外: `<外部ライブラリ境界など限定ケース>`

### 2. エラーハンドリング規約

```
// ✅ 正しい例
<error_handling_good_example>

// ❌ 間違い例
<error_handling_bad_example>
```

- ルール: `<Result型 / 例外の境界 / 独自エラー型>`
- 監査: `<ログ出力時の機微情報マスク>`

### 3. 非同期・並行処理規約

```
// ✅ 正しい例
<async_concurrency_good_example>

// ❌ 間違い例
<async_concurrency_bad_example>
```

- ルール: `<await漏れ禁止 / タイムアウト必須 / リトライ条件定義>`
- 禁止事項: `<無制限並列 / ブロッキングI/O混在 など>`

### 4. データ構造・イミュータブル規約

- ルール: `<readonly / const優先 / コピーオンライト>`
- 禁止事項: `<共有状態の破壊的変更>`

### 5. インポート・モジュール規約

- ルール: `<絶対パスエイリアス / 拡張子 / 循環参照禁止>`
- ルール: `<type import優先 / 副作用importを最小化>`

## 設計パターン

### <推奨パターン名>

- 適用場面: <いつ使うか>
- 責務: <何を担当するか>
- 入出力: <入力型・出力型>

```
<pattern_template_code>
```

## セキュリティ・品質要件

- 入力検証: `<必須チェック内容>`
- 機微情報: `<ログ/例外/レスポンスでの扱い>`
- パフォーマンス: `<N+1回避 / 計算量 / メモリ上限 など>`
- 可観測性: `<メトリクス / トレース / 相関ID>`

## テスト方針

- 単体テスト: `<対象・観点・境界値>`
- 結合テスト: `<外部I/Oを含む観点>`
- 回帰テスト: `<再発防止ケース>`

```
<test_command>
```

## Lint / Format / Typecheck

- Lint: `<lint_command>`
- Format: `<format_command>`
- Typecheck/Compile: `<typecheck_or_build_command>`

## レビュー観点

- 仕様準拠: `<仕様との差分がない>`
- 例外系: `<失敗時の挙動が定義済み>`
- 変更影響: `<既存機能・互換性への影響確認>`

## 変更時チェックリスト

- [ ] 言語ルール違反がない
- [ ] Lint / Format / Typecheck を通過
- [ ] 既存テストと追加テストが通る
- [ ] エラー処理とログ方針に準拠
- [ ] 必要に応じてドキュメント更新

## 記入例（最小）

```yaml
name: TypeScript Development Rules
description: TypeScript 実装の型安全性と品質を統一するための規約
applyTo: "**/*.ts"
```
